import React from 'react';

const BlogShowcase = () => {
  return (
    <div
      style={{
        height: '100vh',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        zIndex: '100',
        color: 'white',
      }}
    >
      blog showcase
    </div>
  );
};

export default BlogShowcase;
